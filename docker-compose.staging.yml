services:
  db:
    env_file:
      - .env.staging
    ports:
      - "127.0.0.1:5435:5432"

  api:
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.staging
    ports:
      - "127.0.0.1:8001:8000"

  worker:
    build:
      context: .
      dockerfile: Dockerfile.prod
    env_file:
      - .env.staging
