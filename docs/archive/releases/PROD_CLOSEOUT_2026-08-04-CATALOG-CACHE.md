# QAZ.FUND — закрытие кеша каталога, 4 августа 2026

## Что выпущено

- Релиз: `5a5dbff` (`avoid background catalog warm-up on startup`).
- Готовый результат повторного запроса `/opportunities` кешируется на 45 секунд.
- Кеш очищается при обновлении данных; ключ учитывает фильтры, язык, страницу и компактный режим.
- Тяжёлый прогрев не выполняется ни синхронно, ни в фоне при старте: готовность контейнера не зависит от сортировки каталога.
- Сжатие gzip из релиза `8948525` сохраняется.

## Проверки

- `PYTHONPATH=. .venv/bin/pytest -q` — 496 тестов пройдено.
- `make lint` — без замечаний.
- `make smoke-prod` — пройдено с первого запуска после перезапуска контейнера.
- `make content-audit` — пройдено: 26 источников, 394 актуальные записи, без stale/unknown и без редакционных нарушений.

## Публичное измерение

На `https://qaz.fund` для стандартного каталога RU (`limit=5000`, `deadline_after=2026-08-04`, `compact=true`) зафиксировано:

- первый запрос после перезапуска: около 2,07 с до первого байта;
- повторные запросы из кеша: 0,32–0,63 с;
- сетевой ответ с gzip: около 88 КБ.

Публичный release marker подтверждает ревизию `5a5dbffb6ddf23da29e9b20cbc83aa0d72b83c41`.

## Границы

Кеш ускоряет одинаковые публичные запросы и не меняет содержимое карточек. Данные остаются неизменяемыми до следующего обновления источников или истечения короткого TTL.
