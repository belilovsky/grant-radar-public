# QAZ.FUND — закрытие наблюдаемости и гигиены, 4 августа 2026

## Релиз

- кодовый релиз: `1d5094a0dac1e6d7ecee964d3fef0595e8ae4ee5`;
- публичный домен: `https://qaz.fund`;
- публичный маркер подтверждает тот же SHA;
- развёртывание: `/opt/grant-radar` на production-host;
- API и база данных после запуска healthy, worker запущен командой `python -m core.scheduler`.

## Что исправлено

Парсеры сохраняют устойчивый контракт: сетевой сбой не останавливает worker и
не обрывает остальные источники. До этого такой сбой мог остаться в логах, а
запуск — ошибочно попасть в историю как успешный. Для этого добавлен единый
`last_fetch_error`: scheduler сохраняет запуск со статусом `error`, не публикуя
ложный успешный результат. Маркер подключён ко всем зарегистрированным
адаптерам; первым реальный случай был обнаружен на UNDP Procurement, после
релиза такой же сигнал подтвердился на UNICEF (HTTP 403).

Успешный запуск и отсутствие новых записей по-прежнему считаются нормальным
состоянием. Ошибка транспорта и ошибка разбора отделены от пустой выборки.

## Проверки

- `501 passed` — полный набор тестов;
- Black, isort, flake8, mypy, vulture — без замечаний;
- `compileall` и `pip check` — без ошибок;
- `pip-audit` для PyPI-зависимостей — `No known vulnerabilities found`;
- локальная зависимость `qazstack==1.40.0` поставляется намеренно из
  `vendor/qazstack-1.40.0-py3-none-any.whl` и отсутствует в индексе PyPI;
- production smoke: `status=ok`, `394` релевантные открытые записи, `26`
  включённых источников, stale/unknown источников `0`;
- production smoke дополнительно проверяет HTML opportunity/funder pages,
  языковые оболочки и cache-control этих публичных маршрутов;
- внутреннее имя QazCompute убрано из пользовательского текста: показатель
  полноты данных оставлен, но описан обычным языком;
- штатный тайм-аут полного последовательного smoke-прогона увеличен до 60 с:
  холодный origin может обрабатывать весь маршрутный набор дольше 20 с при
  сохранении корректных ответов;
- operator health после восстановления UNICEF: `status=ok`, unresolved
  `failed_runs=[]`; исправленная ошибка остаётся в `recent_runs` для аудита;
- браузерные проверки desktop/mobile для RU, ҚАЗ и EN: переполнения и
  консольные ошибки не обнаружены.

## Остаточный риск

Публичная свежесть источника считается по последнему успешному запуску и может
оставаться `fresh` в пределах допустимого окна после отдельного сбоя. Поэтому
операторская проверка должна учитывать оба сигнала: `coverage` для давности
данных и `operator/health` для последних ошибок запусков. Это разделение
сохранено намеренно, чтобы кратковременный сбой не скрывал ранее проверенные
данные от посетителей и одновременно не терялся в операторском контуре.
